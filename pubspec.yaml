name: media_selector
description: instagrammable media selector.
publish_to: none
version: 0.0.7
homepage:

environment:
  sdk: ">=2.15.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  photo_manager: ^1.3.10
  provider: ^6.0.1
  crop: 
    git: 
      url: https://github.com/ukkoon/flutter_crop.git
      ref: v0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter    
  flutter_lints: ^1.0.0  

flutter: